var mylayer = app.activeDocument.activeLayer ;
var num = prompt("Enter a Value : " , 10 , "Num");

for (var i = 0; i < num; i++) {
	
	var newlayer = mylayer.duplicate();
	newlayer.translate(i,i);
    alert(Math.sin(3.1415));

};

